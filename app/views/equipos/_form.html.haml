= form_for @equipo do |f|
  - if @equipo.errors.any?
    #error_explanation
      %h2= "#{pluralize(@equipo.errors.count, "error")} prohibited this equipo from being saved:"
      %ul
        - @equipo.errors.full_messages.each do |msg|
          %li= msg
  .container-form
    .field
      = f.text_field :nombre, :placeholder=>"Nombre", :class=>'input-form'
    %br/
    .field
      = f.collection_select(:tipo_equipo_id, TipoEquipo.all(:order => 'nombre'), :id, :nombre, { :prompt => 'Selecionar Tipo de Equipo' }, :class=>'input-form')
    %br/
    .field
      = f.text_area :descripcion, :placeholder=>"Descripción", :class=>'input-form'
  .modal-footer.footer-login
    .actions
      = f.submit 'Guardar Cambios', :class=>'btn btn-app' 
